# 物理学习 OS — 全面优化方案 V2.3（对标全网最佳实践，全项采纳）

> 基于 2026 年 8 月全网调研（OpenTutor / physics-learning-agent / Synedrix / MentorMind / AI错题本 / 考研错题集 / 智卷 / Nexus AI / socratiskill / ScaffoldLM / FSRS 生态 / Interleave 等 15+ 项目），提炼 28 项优化。
> **V2.1 修订说明（2026-08-11）**：依据项目 `AGENTS.md` 四条强制红线对 V2 全文做合规性审核，修订如下——
> ① 新增「〇、合规红线与依赖策略」章节（最高优先级）；
> ② A1 依赖改为 vendored 引入（禁止全局 pip）；D1 弃用 OS 密钥环（写系统存储违反 R1），改为工作区内加密文件；B2 OCR / C3 Ollama / F3 Transformers.js 全部标注「用户主动安装/启用」边界；
> ③ 修正技术细节：D3 以 Anki-CSV 为主（自写 apkg 降级为次要）、B1 注明 `capture` 仅移动端生效、C1 补充 SSE 错误推送协议、A2 补种子概念表、B4 补版权合规条款；
> ④ 新增「十一、已落地项」记录本轮修复。
> **V2.2 修订说明（2026-08-11）**：对照代码库现状逐条核对，修正 8 处方案与实现不符——
> ① 表名修正：Schema 中 `cards` 全部改为实际表名 `problems`（`config.py` SCHEMA）；
> ② A3：`error_type` 字段已存在（`config.py:93`），方案改为「已有字段升级为受控枚举 + 新增结构化字段」，非新增列；
> ③ C1：**代码中没有 `/api/chat` 端点**，AI 交互实为 `POST /api/problems/{id}/hint` 与 `POST /api/oral/respond`；SSE 改造对象改为这两个端点；
> ④ D1：密钥实际存于 `settings` 表（DB）且已支持环境变量优先（`db.py:115`）——**现状违反 R4「密钥绝不写入数据库」**；V2.2 新增「密钥出库」迁移方案；
> ⑤ D3：`/api/export`（JSON 全量导出）已存在（`handler.py:226`），方案改为「在现有导出上扩展 Anki-CSV/.ics」；
> ⑥ D4：`/api/dashboard` 与 `/api/trend` 已存在（`handler.py:80,108`），方案改为「扩展仪表盘」而非新增；
> ⑦ F1 现状确认：口试五轮已实现（`oral.py` 五轮追问），方案描述保持；
> ⑧ A7 现状修正：「今日复盘」当前无独立功能，复习队列按 due_date 排序（`app.js:571`），方案保留。
> **V2.3 修订说明（2026-08-11）**：第三轮全文一致性审核，修正 13 处——
> ① 总数修正：总览实为 **28 项**（A8+B5+C6+D6+F3），非 25；
> ② V2.1 说明中「十一、已落地项」实际章节为「九」，修正引用；
> ③ 红线表 R4 行「密钥存储默认内存（现状）」与 V2.2 发现矛盾，改为「迁移至 keys.enc」；
> ④ 红线表 R3 行 AI 写入清单补 A3/A8/F1；
> ⑤ 模块 E 标题与实际编号 F1-F3 不符，改为「模块 F」；
> ⑥ A6 补现状：三级提示已存在（`/api/problems/{id}/hint` + 前端 hint1/2/3 按钮），方案为「扩为四级阶梯 + 诊断门」；
> ⑦ C3 改为优先复用 Ollama OpenAI 兼容端点（`http://localhost:11434/v1`，走现有 `ai.py` 逻辑），原生 `/api/chat` 仅作可选直连；
> ⑧ 总览 B3 依赖「sqlite-vec」改为「SQLite FTS5」（详情一直写 FTS5，总览笔误）；
> ⑨ 总览 D1 名称与详情「密钥出库迁移」对齐；
> ⑩ 验收 A2/A3/A4 拆开，新增 B5 验收（「80% 绑定」实属 B5 功能）；
> ⑪ 验收 R3 清单去掉无 AI 写入的 B4，补 A3/A8；
> ⑫ 九、已落地项「打卡按钮」注记：当前代码无打卡功能（static 无 checkin），该条为历史会话修复记录，保留原述；
> ⑬ 概述标题「二、优化项总览」计数核对通过（28 行）。
> **前提约定**：本方案不考虑工程量，按"价值排序"给出完整设计；执行时可分批落地。
> **架构原则**：保留「本地优先 + 数据自持」基因；所有 AI 能力必须有**离线降级路径**；所有依赖引入必须遵守下方合规红线。

---

## 〇、合规红线与依赖策略（V2.1 新增，最高优先级）

本方案所有条目必须满足项目 `AGENTS.md` 四条强制约束。逐条映射如下：

| 红线 | 对方案的影响 | 本方案如何满足 |
|---|---|---|
| **R1 工作区隔离**：所有修改/文件/运行产物严格限制在 `E:\tool\biancheng\AI project 2\physics-study-os` 内 | 依赖、模型、临时数据不得落到系统目录；不得安装全局依赖；不得写系统存储（注册表/凭据管理器） | 依赖统一以 **vendored** 方式放工作区 `vendor/`；模型放工作区 `models/`；测试临时数据 `tests/.tmp/`（已落地）；构建产物在 `build/`、`dist/` 内 |
| **R2 零第三方依赖主路径** | 主路径只用 Python 标准库；可选依赖缺失时必须降级 | 每项标注「引入方式 + 缺失降级路径」；vendored 依赖激活条件 = 文件存在，不存在即走标准库实现 |
| **R3 AI 写入管控** | AI 生成内容不得静默落库 | 所有 AI 写入走「草稿确认」（A3/A4/A8/B1/B5/F1）或「结构化校验」（C4）；校验失败走离线模板 |
| **R4 密钥安全** | 密钥绝不写入数据库；日志脱敏 | **V2.2 发现现状违规**（api_key 存 settings 表）→ V2.3 迁移方案：工作区加密文件 `data/keys.enc` + 环境变量优先链（D1）；日志脱敏 redactor 保持；**不采用 OS 密钥环**（写系统存储违反 R1） |

### 依赖引入方式规则

| 方式 | 适用 | 细则 |
|---|---|---|
| **vendored（首选）** | py-fsrs、任何纯 Python 库 | 把源码拷入 `vendor/<pkg>/`（保留 MIT 许可声明），`sys.path` 注入；**绝不 `pip install` 全局** |
| **用户主动安装（可接受）** | Ollama、OCR 模型、浏览器端 Transformers.js | 安装/下载动作由**用户主动执行**（用户行为非本项目行为）；项目仅探测可用性并在缺失时降级；模型文件落点由用户指定或浏览器管理 |
| **禁止** | pywin32 密钥环、系统级组件 | 触碰 R1（写系统存储/注册表）或 R2（无降级） |

### 可选依赖清单

| 依赖 | 引入方式 | 激活条件 | 缺失降级 |
|---|---|---|---|
| py-fsrs | vendored `vendor/py_fsrs/` | 目录存在 | 回退现有 SM-2 实现 |
| Ollama | 用户自装（外部进程） | `/api/tags` 探测成功 | 仅云端 API 可用 |
| 视觉模型（云端 API） | 用户配置 vision 模型 | 设置页已填且支持图片输入 | 保存图片 + 手动录入 |
| OCR（paddleocr 等） | 用户主动安装 | 可导入 | 浏览器本地 OCR → 手动录入 |
| Transformers.js（F3） | 浏览器 CDN + 浏览器缓存模型 | 用户显式启用 | 关键词搜索（现状） |

---

## 一、对标项目速览

| 项目 | 定位 | 借鉴点（对应优化项） |
|---|---|---|
| **OpenTutor** (67★) | 本地自托管自适应学习工作台 | FSRS-4.5（A1）、LOOM 记忆图谱（A2）、LECTOR 语义复习（A2）、认知负荷检测（A6）、BKT 追踪（A3） |
| **physics-learning-agent** | LangGraph 大学物理学习工作区 | 原创新题生成（A4）、个人资料 RAG（B3）、意图分类（C5）、双语（F2） |
| **Synedrix** | 个人学习 OS（定位最接近） | 五系统共享掌握度上下文（C5）、错误日志喂复习队列（A3）、AI 结构化输出校验（C4） |
| **MentorMind** | 自托管 AI 助教 + 真题锚定 | 真题语料 RAG 锚定评分标准（B4）、Anki/CSV 导出（D3）、.ics 计划（D3）、PWA 推送（D2）、防刷打卡（D6） |
| **Sif** | 物理化学学习 App | 课堂 → 练习 → 全局卡组流水线（F1）、模块可读进度（D4） |
| **bantaba** | 离线苏格拉底辅导（微调模型） | 引导问题显式训练（F1）、内部思考后作答（F1） |
| **ScaffoldLM** | 规划引导式辅导框架 | 逐步教学计划（A6）、按步骤追踪认知状态（A3） |
| **AI错题本 / 考研错题集 / 智卷** | 中文错题本三件套 | 拍照录题+视觉分析（B1）、举一反三（A4）、今日复盘/再练（A7）、错因分布（A3）、试卷 OCR（B2）、SSE 流式（C1） |
| **Nexus AI** | 记忆驱动学习引擎 | Hindsight 结构化记忆（C5）、错因分类反馈（A3）、自适应出题（A4） |
| **socratiskill** | 自适应苏格拉底插件 | 级别校准状态机+诊断门（A6）、Feynman 模式（A5）、Leitner（F1）、学习周报（D5） |
| **FSRS 生态** (py-fsrs) | 下一代间隔复习算法 | FSRS-6 机器学习调度（A1）、目标保持率控制（A1） |
| **Interleave / MarkItUp / Atlas** | 知识管理 + 复习 | 增量阅读溯源（B3）、浏览器端语义搜索（F3）、FTS5 检索（D4） |
| **EduClaw / ai-tutor** | 本地优先辅导终端 | 双档模型 Fast+Heavy（C2）、密钥安全存储（D1）、游戏化 XP（D6） |
| **math-education-agent** | K12 数学智能 Agent | 一题多解（A8）、口诀助记（A8）、知识图谱可视化（A2）、真题练习（B4） |
| **gongkao-mistake-review** | 错题结构化卡片 PWA | 错题字段模型：错误路径/陷阱/捷径公式（A3）、AI 整理草稿人工确认（B1） |

---

## 二、优化项总览

| 编号 | 优化项 | 模块 | 价值 | 依赖 | 阶段 |
|---|---|---|---|---|---|
| A1 | SM-2 → FSRS-6 间隔复习 | 算法 | ★★★★★ | py-fsrs（vendored） | 1 |
| A2 | 概念知识图谱（LOOM/LECTOR） | 算法 | ★★★★★ | 自研 | 2 |
| A3 | 错因结构化画像 + 定向反馈 | 算法 | ★★★★★ | AI | 1 |
| A4 | 举一反三变式题引擎 | 算法 | ★★★★★ | AI | 2 |
| A5 | Feynman 口述反转模式 | 算法 | ★★★★ | AI | 2 |
| A6 | 自适应提示阶梯 + 诊断门 | 算法 | ★★★★ | AI | 2 |
| A7 | 交错练习 + 今日复盘 | 算法 | ★★★★ | 无 | 1 |
| A8 | 一题多解 + 口诀助记 | 算法 | ★★★ | AI | 3 |
| B1 | 拍照/截图录题 + 视觉错因分析 | 输入 | ★★★★★ | 视觉模型（用户配置） | 2 |
| B2 | 试卷 PDF 批量导入（OCR） | 输入 | ★★★★ | OCR（用户自装） | 3 |
| B3 | 个人资料 RAG（教材/课件/笔记） | 输入 | ★★★★★ | 自研（BM25 主 + SQLite FTS5 可选） | 2 |
| B4 | 真题对齐 + 考试就绪度 | 输入 | ★★★★ | 用户自有语料 | 3 |
| B5 | 自动标签 + 知识提取 | 输入 | ★★★★ | AI | 1 |
| C1 | SSE 流式输出 | AI 工程 | ★★★★ | 自研（标准库） | 1 |
| C2 | 双档模型（Fast/Heavy） | AI 工程 | ★★★★ | 配置 | 1 |
| C3 | 本地模型支持（Ollama） | AI 工程 | ★★★★ | Ollama（用户自装） | 2 |
| C4 | 结构化输出校验 | AI 工程 | ★★★★ | 自研轻量校验（不引 jsonschema 包） | 1 |
| C5 | 学习者档案（持久化画像） | AI 工程 | ★★★★★ | 自研 | 2 |
| C6 | AI 调用遥测 | AI 工程 | ★★★ | 自研 | 3 |
| D1 | 密钥安全存储（密钥出库迁移） | 体验 | ★★★★ | 自研（标准库 hashlib，cryptography vendored 可选） | 1 |
| D2 | PWA + 复习推送 | 体验 | ★★★★ | ServiceWorker（纯本地） | 3 |
| D3 | Anki/CSV/.ics 导出 | 体验 | ★★★ | 自研 | 2 |
| D4 | 复习分析仪表盘（遗忘曲线等） | 体验 | ★★★★ | 自研 | 2 |
| D5 | 学习日志周报 | 体验 | ★★★ | AI | 3 |
| D6 | 游戏化防刷（打卡/XP） | 体验 | ★★★ | 自研 | 3 |
| F1 | 口试五轮升级为苏格拉底引擎 | 增强 | ★★★★ | AI | 2 |
| F2 | 双语/多语言支持 | 增强 | ★★★ | AI | 3 |
| F3 | 语义搜索（浏览器端向量） | 增强 | ★★★ | Transformers.js（用户启用） | 3 |

## 三、详细方案

### 模块 A：学习算法（价值最高）

**A1 SM-2 → FSRS-6 间隔复习**
- 现状：自研 SM-2 变体（`review.py`），单一 EFactor 漂移。
- 目标：FSRS-6（py-fsrs 算法，机器学习预测记忆保持率）。
- 实现：
  - `vendor/py_fsrs/`（vendored，保留 MIT LICENSE；**不 pip 全局安装**）；导入失败 → 现有 SM-2 完全保底。
  - `reviews` 表（现有表，非新增）补 FSRS 调度列：`state, stability, difficulty, due, last_review, elapsed_days, scheduled_days, review_log(JSON)`；`problems.ease_factor/repetition` 保留作迁移来源（迁移现有卡片：用 SM-2 历史回填 `recall`，冷启动期间 py-fsrs 直接读历史）。
  - 目标保持率滑块 0.80–0.95（默认 0.90），调度表缓存到 `cache/`（避免每次全量重算）。
  - 导出 FSRS 标准 CSV（`.csv`：card_id, review_date, rating 0-4）→ 官方 FSRS-Console 可复算参数。
- 降级：vendored 包缺失 → 自动回退 SM-2，功能不变（V2.1 明确此边界）。

**A2 概念知识图谱（LOOM/LECTOR）**
- 现状：知识点是平铺标签，无层级、无依赖。
- 目标：概念 → 母知识点 → 章节 → 单元 四层图谱；图谱驱动「相关联错题」「复习子序列」。
- 实现：
  - 新增表 `concepts(id, name, parent_id, chapter_id, difficulty, mastery_est, looms_in)`；`concept_links(concept_a, concept_b, relation[prerequisite|related|contrast])`。
  - 种子数据：按人教/鲁科/教科版教材章节目录，预置 300+ 概念与先修关系（`seed_concepts.json`，V2.1 补充：解决冷启动图谱空白的 UX 问题；用户可在图谱上增删改，改动不落盘到 seed，存用户表）。
  - 每次答题：题-概念绑定（A3 自动标签）→ 图谱掌握度概率传播（先修不熟 → 子概念打折）。
  - 可视化：`concept_map.html`，d3 式力导向图（本地渲染），点击概念 → 关联错题 + 建议复习序列。
- 先修检测：答题时提示「先修概念 ×× 掌握度低（42%）」，并支持一键「先修模式」过滤出先修相关的历史错题。

**A3 错因结构化画像 + 定向反馈**
- 现状：`problems.error_type` 已存在（`config.py:93`，自由文本默认「待诊断」），但无结构化画像。
- 目标：错因升级为受控枚举 + 结构化字段（借鉴 gongkao-mistake-review 字段模型）：错误类型、错误路径、陷阱点、捷径公式/结论、正确思路关键步骤。
- 实现：
  - `error_type` 保持列名，值域约束为枚举（`concept_misunderstood|calculation|careless|time_pressure|misread|blank_in_facts|heuristic_trap`），已有自由文本做一次归并迁移；新增 `error_path(JSON)`、`trap_note`、`shortcut`、`fix_action` 列。
  - 提交错题时 AI 自动补全；用户「确认/修正」后才落库（**R3 草稿确认**）。
  - 每 7 天生成「错因画像」：类别占比、每类对应的策略卡（如 calculation → 限时草稿纸规范；careless → 易错自检清单）。提供「今日只练 careless 类」入口。
  - 错题修复校验：修复题做对 → 画像更新；连续修复失败 → 推荐回 A6 阶梯重讲。
- 冷启动：无 AI 时手选 7 类下拉 + 自填文本（结构化校验仅做必填校验）。

**A4 举一反三变式题引擎**
- 目标：错题 → 自动生成 3 道变式（数值替换 / 情境替换 / 反向设问）+ 难度自适应（结合 A1 保持率与 A2 掌握度）。
- 实现：
  - 模板：每个「题根」存 `variants[]`（人工或 AI 预生成）；生成规则 = 参数化替换（数值范围按错误类型：计算错 → 小数更丑；概念错 → 换情境表述）。
  - AI 生成走「草稿 → 双校验」（语法校验 + 官方答案一致性校验，见 C4）；不一致 → 不落库。
  - 变式正确率回写题根质量分，低分题根自动降权。
- 降级：离线模板库（按题型×难度×错因预置参数模板）。

**A5 Feynman 口述反转模式**
- 模式：对错题做「向新手讲解」口述 → 先讲概念（不读公式）→ 对照标准解析找漏点 → 生成自评表（漏点 / 讲错点 / 讲清点）。
- 实现：口试引擎加 `feynman` 模式（复用 F1 引导流程）；语音可选；自评表入库，下次复习优先重考漏点。

**A6 自适应提示阶梯 + 诊断门**
- 现状：三级提示已存在（`/api/problems/{id}/hint` + 前端 hint1/2/3 按钮，`app.js:358-360`），为固定三级。
- 目标：扩为四级阶梯（按需展开，每次只露一级）：① 提示关键词 ② 提示下一步公式/定理名 ③ 展示例题骨架 ④ 全解析。
- 状态机（借鉴 socratiskill 校准）：`check -> scaffold -> diagnose`；对同一错因重复失败 → 自动降级到低一档提示并触发「诊断会话」（AI 出 3 题判断是概念还是方法问题）。
- 记录阶梯使用轨迹（JSON），供周报统计「卡在哪一级」。

**A7 交错练习 + 今日复盘**
- 交错：复习队列按「知识点混合 + 题型混合」排序（默认关闭，可开）；新增复习卡片时混合先修/相关概念。
- 今日复盘：每日答完最后一张卡后，自动生成「今日错因摘要 + 明早 3 卡温习」；支持 `复盘提醒` 日程。
- 无 AI 依赖：摘要用模板拼装（标签统计 + 模板句）。

**A8 一题多解 + 口诀助记**
- 每题存 `methods[]`（多解法，AI 生成+人工确认）；复习时「换解法重做」；对高频易错点生成 4-6 字口诀（如「南左北右」「增反减同」），置于卡片答案区顶部。

### 模块 B：输入管线

**B1 拍照/截图录题 + 视觉错因分析**
- 拍照（`capture` 属性，V2.1 注明：**仅移动端生效**）或截图（粘贴 Ctrl+V，桌面端）+ 图片入库 `media/`。
- 视觉模型（用户配置的 vision 能力，如 qwen-vl）识别题目 → 生成卡片草稿（题干/选项/答案/解析）→ **人工确认后落库**（R3）。
- 视觉错因分析：拍照草稿纸 → 识别错误步骤位置 → 标注入错步骤（须用户确认）。
- 降级：无 vision → 图片仅作为附件 + 手动录入。

**B2 试卷 PDF 批量导入（OCR）**
- 多页 PDF → 每页切图 → OCR（**paddleocr 等由用户主动安装**，V2.1 明确此边界）→ 结构化题目卡（题号/题干/选项/分值/时间）。
- 关键联动：OCR 完成后直接跑「今日复盘」与「错因画像」。
- 降级链：OCR 缺失 → 浏览器本地 OCR（若有）→ 手动逐题录入模板（无 OCR 时全手动）。
- 版权合规（V2.1 新增）：仅接受**用户自有**试卷（扫描件/自建题库），不内置第三方题库；导入物仅存工作区。

**B3 个人资料 RAG（教材/课件/笔记）**
- 摄取：用户指定工作区内目录（教材 PDF、课件、笔记 md/txt）；解析器：PDF→文本（pdfminer vendored 可选 / 纯文本提取）、md/txt 直读。
- 检索：**BM25 为主（标准库实现，零依赖）**；可选 SQLite FTS5（SQLite 自带，无第三方依赖）。
- 用途：解答时「基于你的教材第 3 章 2.1 节」引用出处；错题解析自动关联教材片段。
- 溯源：引用必须带 `来源路径 + 页码`，点击跳转打开对应文件（增量阅读式溯源）。

**B4 真题对齐 + 考试就绪度**
- 用户导入真题（自有版权，合规条款同上）→ 真题库与错题互链 → 按考点统计「真题命中度」。
- 就绪度 = Σ(真题考点权重 × 掌握度) / Σ权重；仪表盘展示「距考试目标还差 ××」。

**B5 自动标签 + 知识提取**
- 新题入库时 AI 自动打标签（知识点/题型/难度/错因）+ 绑定概念图谱（A2）。
- 草稿确认制（R3）；置信度 < 0.9 的标签标黄待确认。
- 降级：现有关键词标签规则（按题干词频）。

### 模块 C：AI 工程

**C1 SSE 流式输出**
- 现状：AI 交互端点 `POST /api/problems/{id}/hint`（提示）与 `POST /api/oral/respond`（口试）均为一次性 JSON 返回；长解析等待且无打字机效果。
- 目标：两端点升级为 SSE（`text/event-stream`），事件协议：`[start|delta|sources|done|error]`；V2.1 补充：**响应头已发送后出错 → 发 `error` 事件（含 `partial` 字段标识已完成部分），前端合并展示并提示重试**。
- 实现：标准库 `http.server` + 生成器；前端 `fetch` + `ReadableStream` 逐块渲染；支持中断（AbortController → 后端关闭生成器）。
- 兼容：老客户端 Accept 非 SSE → 仍返回完整 JSON（内容协商）。

**C2 双档模型（Fast/Heavy）**
- 配置两档：Fast（轻量，问答/标签/抽取）+ Heavy（推理/变式/苏格拉底）。
- 按任务路由；Fast 超时 → 提示或降级离线；Heavy 失败 → 尝试 Fast。

**C3 本地模型支持（Ollama）**
- 探测 `http://localhost:11434/api/tags` → 设置页显示「本地模型可用/不可用」；不可用时隐藏本地选项。
- **首选：复用 Ollama OpenAI 兼容端点**（`http://localhost:11434/v1`，走现有 `ai.py:80` 的 api_endpoint 逻辑，零改动对接）；原生 `/api/chat` 仅作可选直连（进阶）。
- Ollama **由用户自行安装**（V2.1 明确：项目不下载/不安装/不启动该进程，仅以外部进程调用）；API 密钥不适用于本地模型（空密钥走本地）。

**C4 结构化输出校验**
- AI 返回 JSON 先过轻量校验器（自研：JSON 语法解析 + 必填字段 + 类型 + 枚举约束 + 数值区间；**不引入 jsonschema 第三方包**，R2）。
- 校验失败：≤2 次自动重试（附错误信息）；仍失败 → 走离线模板/返回错误码 `AI_SCHEMA_ERROR`，不落库（R3）。

**C5 学习者档案（持久化画像）**
- 持久化维度：掌握度曲线（按知识点）、错因分布、学习节奏（时段/频率）、偏好（讲解深度/例题数）、目标（考试日期/目标分）。
- 全局上下文：所有 AI 请求自动附带档案摘要（token 受限，约 800 token 快照）。
- 隐私（V2.1 新增）：档案默认仅在本地展示；导出到任何外部服务前须用户确认，不随遥测外发（C6 不含档案）。

**C6 AI 调用遥测**
- 表 `ai_telemetry(ts, route, model, latency_ms, tokens, ok, error_kind)`；**不含任何 prompt/内容/答案**（V2.1 明确内容级隐私）。
- 仪表盘：token 消耗、失败率、慢请求 TOP、模型对比；滚动清理策略：**保留 90 天**（V2.1 明确，防数据膨胀）。

### 模块 D：体验与工程

**D1 密钥安全存储（密钥出库迁移）**
- V2.1 变更：弃用 OS 密钥环（写系统存储违反 R1）。
- **V2.2 关键发现（R4 违规修正）**：现状 `api_key` 存于 `settings` 表（DB），仅显示层脱敏（`db.py:115`）——违反 R4「密钥绝不写入数据库」。迁移方案：
  1. `settings` 表删 `api_key` 行（保留 `has_api_key` 标志位），存量密钥迁移到工作区加密文件 `data/keys.enc`（gitignore；scrypt/PBKDF2 派生密钥 + AES-GCM；主密钥 = 用户口令 或 环境变量 `PSO_MASTER_KEY`）；
  2. 环境变量优先链保持不变：`PHYSICS_OS_API_KEY`（现有，`db.py:118`）→ keys.enc → 内存；
  3. 日志脱敏：密钥一律 `***`（R4，redactor 已有 `config.py:44`，保持）。
- 降级：无加密依赖 → 内存密钥（每次启动重输）。
- 校验：迁移后 `settings` 表 `SELECT *` 不含任何密钥明文。

**D2 PWA + 复习推送**
- manifest.json + ServiceWorker（纯本地）：本地 Notification 定时提醒；离线可用（appshell 缓存）。
- 移动端添加到主屏幕；局域网 HTTP 部署时 ServiceWorker 受限（HTTPS 限制），标注降级路径。

**D3 Anki/CSV/.ics 导出**
- 现状：`GET /api/export` 已有（JSON 全量导出，`handler.py:226`，只读）。
- 目标：在同一端点扩展 `format` 参数：`anki-csv`（主路径，字段映射 question/answer/tags/due）、`ics`（复习日程）；保留现有 JSON 格式为 `format=json`。
- 双向：支持从 Anki-CSV 导入（映射同上）。
- V2.1 修订：自写 apkg 降级为「可选实验」，不作为主路径（避免随 Anki 格式漂移维护）。

**D4 复习分析仪表盘**
- 现状：`/api/dashboard`（统计卡片）与 `/api/trend`（掌握度趋势，`app.js:265` 已渲染折线）已存在。
- 目标：扩展——遗忘曲线（复习点 + FSRS 预测曲线）、保持率趋势、未来 7 天复习压力（due 分布）、卡组健康度（新生/学习/毕业/卡片年龄分布）、知识点掌握热力（A2 图谱联动）。
- 纯本地渲染（canvas/SVG），不引第三方图表库。

**D5 学习日志周报**
- 每周日自动生成：投入、新增/复习/毕业卡数、错因变化、目标进度、下周建议（AI 摘要或模板统计降级）。

**D6 游戏化防刷**
- 防刷（借鉴 MentorMind）：打卡须当日 ≥1 卡完成；每日卡片上限；闪电复习不计 XP。
- XP 按 FSRS rating 结算；streak 加成；徽章本地计算；无排行榜（隐私）。
- 无内购（不存在花钱刷 XP 路径）。

### 模块 F：增强功能

**F1 口试五轮 → 苏格拉底引擎**
- 校准级别状态机（socratiskill 借鉴）→ 逐步引导问题（ScaffoldLM）→ 内部思考后作答（bantaba 借鉴）→ 会话回写画像（A3/A5）。
- 流水线（Sif 借鉴）：口试暴露薄弱点 → 自动生成复习卡。

**F2 双语/多语言**
- 界面字符串抽离 `locale/`（zh-CN 现有 + en-US 起步）；术语表中英对照；AI 支持「用英文讲解」。

**F3 语义搜索（浏览器端向量，可选）**
- 用户显式启用后：浏览器端 Transformers.js 生成向量（模型存浏览器缓存，**不下载进工作区**）；IndexedDB 存向量 + 余弦检索。
- 降级：关键词搜索（现状）；默认关闭（省流量/省资源）。

---

## 四、Schema 变更清单

| 表 | 变更 |
|---|---|
| `problems`（现表） | +`error_path, trap_note, shortcut, fix_action`（A3）；+`methods`（A8）；+`concept_ids`（A2）；`error_type` 枚举约束 + 归并迁移（A3）；+`ai_pending` 草稿标记（B1/B5/R3） |
| `reviews`（现表） | +`state, stability, difficulty, due, last_review, elapsed_days, scheduled_days, review_log(JSON)`（A1）；+`scaffold_used`（A6） |
| 新表 | `concepts` + `concept_links`（A2）；`concept_progress`（A2）；`ai_telemetry`（C6）；`learner_profile`（C5）；`variants`（A4）；`ai_drafts`（R3 草稿池）；`exam_plan` + `question_banks`（B4） |
| 迁移 | 只增列（默认值回填），不删列；SM-2 历史数据无损（A1）；`settings.api_key` 出库到 `data/keys.enc`（D1，R4 合规） |

## 五、API 变更摘要

- `POST /api/problems/{id}/hint`、`POST /api/oral/respond` → SSE 升级（C1），非 SSE 客户端兼容 JSON（内容协商）。
- `POST /api/ai/extract` → 草稿返回（B1/B5）；`POST /api/ai/confirm|reject` → R3 确认端点（统一收口）。
- `POST /api/variants/generate|confirm`（A4）；`GET /api/graph/concepts`（A2）。
- `POST /api/upload/photo|pdf`（B1/B2）；`GET /api/models/probe`（C3）。
- `GET /api/export?format=anki-csv|ics|json`（D3，扩展现有端点）；`GET /api/dashboard/*` 扩展（D4）；`GET /api/report/weekly`（D5）。
- 新错误码：`AI_SCHEMA_ERROR`、`AI_UNAVAILABLE`、`SSE_ABORT`。

## 六、分阶段路线图

| 阶段 | 内容 | 估时 |
|---|---|---|
| **1（算法 + 工程基座）** | A1、A3、A7、B5、C1、C2、C4、D1、D3 | 2-3 周 |
| **2（智能增强）** | A2、A4、A5、A6、B1、B3、C3、C5、D4、F1 | 4-6 周 |
| **3（体验与生态）** | A8、B2、B4、C6、D2、D5、D6、F2、F3 | 3-4 周 |

## 七、验收标准

- **A1**：调度与 py-fsrs 参考输出偏差 <5%；SM-2 数据无损迁移；vendored 移除后自动回退 SM-2（功能不中断）。
- **A2**：种子图谱 300+ 概念可加载；先修传播正确（3 类用例：无先修/先修不熟/对比关系）；图谱页可视化渲染。
- **A3**：错因枚举迁移无数据丢失；画像 7 天可生成；`error_type` 值域校验拒绝非法值。
- **A4**：变式生成校验通过率 >95%（不通过不落库）；离线模板库在无 AI 时可用。
- **B5**：新题 80%+ 自动绑定概念；草稿确认制生效（未确认不落库）。
- **C1**：SSE 断线/出错 → `error` 事件（含 partial）；重连不重复落库；非 SSE 客户端 JSON 兼容。
- **C3**：Ollama 缺失 → 界面隐藏本地选项，主流程不受影响；本地端点经 OpenAI 兼容层可直连。
- **R3 全面**：AI 写入全链路（A3/A4/A8/B1/B5/F1）必经 `confirm` 端点，无旁路。
- **隔离红线**：全新环境仅拷工作区 → 启动/测试/导出不产生工作区外文件；`pip list` 无全局新增依赖；测试产物在 `tests/.tmp/` 且 git 干净。

## 八、风险与缓解

| 风险 | 缓解 |
|---|---|
| FSRS 冷启动 | SM-2 历史回填 recall；种子概念表（A2） |
| AI 不可用 | 全链路离线降级 + 草稿确认不阻塞录入 |
| 图片/PDF 膨胀 | `media/` 限额提示 + JPEG 压缩（质量 80%） |
| 遥测/档案隐私 | 不含内容、本地存储、90 天清理、导出确认（C5/C6） |
| vendored 依赖漂移 | 固定版本 + 保留 LICENSE；升级走变更流程 |

## 九、已落地项（2026-08-11 本轮）

| 项 | 说明 |
|---|---|
| 3 个前端缺陷修复 | 含打卡按钮 JS 逻辑错误（注：V2.3 核实当前 static 无打卡功能，此为历史会话修复记录） |
| 前端版本参数修正 | `?v=` 缓存失效问题 |
| 移除未使用变量 | `theme.js` 等 |
| OPTIMIZATION_PLAN 路径校验修复 | 文件名与运行路径一致 |
| 测试临时数据隔离 | `tests/.tmp/`（工作区红线） |
| 清理 `.bak` 残留 | 构建期文件 |
| stdout UTF-8 强制 | Windows 控制台中文乱码 |
| AGENTS.md 项目规则 | 四条红线 + 降级 + 密钥纪律 |
| 本方案 V2.1 | 合规审核 + 修订（本文档） |
| 本方案 V2.2 | 对照代码现状 8 处修正（表名/error_type/SSE 端点/密钥出库/导出与仪表盘现状） |
| 本方案 V2.3 | 全文一致性 13 处修正（28 项计数/R3·R4 红线表/模块 F/A6 现状/C3 兼容端点/B3 依赖/验收拆分） |
